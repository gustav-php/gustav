#!/usr/bin/env php

<?php

[$current, $port, $entrypoint] = $argv;

$port ??= "1337";
$entrypoint ??= "/index.php";
$entrypoint = realpath(getcwd() . "/" . $entrypoint);

passthru(PHP_BINARY . " -S 0.0.0.0:{$port} $entrypoint 2>&1");